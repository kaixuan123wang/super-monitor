# monitor-web

JS 监控平台前端（Vue 3 + Vite + Element Plus）。

## 启动

```bash
pnpm install
pnpm dev          # http://localhost:3000，自动代理 /api -> http://localhost:8080
pnpm build
```

Phase 1 仅搭建基础布局与路由骨架，各业务页面会在后续阶段完善。
